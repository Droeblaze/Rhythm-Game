public enum ControlProfile
{
    FightStick,  // PRIMARY: arcade stick — dedicated fight stick face button layout
    Gamepad,     // FALLBACK: standard Xbox/PS controller face button layout
    Keyboard     // FALLBACK: keyboard directional keys + key rows for button lanes
}
